#!/bin/bash

. ~/.genpatchesrc

name="${ECHANGELOG_USER%<*}"
email="${ECHANGELOG_USER#*<}"
email="${email%>}"

cd ${WEB_LOCAL}
./email-announcement.pl $@ |
	${SENDMAIL} -F \"${name}\" -f \"${email}\" gentoo-kernel@lists.gentoo.org


